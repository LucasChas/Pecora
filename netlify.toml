# Configuración de deploy para Netlify.
[build]
  command = "npm run build"
  publish = "dist"

# SPA: cualquier ruta (ej. /admin) la resuelve React Router, no el servidor.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
